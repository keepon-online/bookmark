# 智能书签插件架构设计文档

**设计日期**: 2025-01-19
**版本**: v1.0
**设计师**: Claude (Sonnet 4.5)

---

## 1. 系统概述

### 1.1 产品定位

智能书签插件是一款基于 AI 的浏览器扩展，旨在解决用户书签管理混乱、检索困难、链接失效等核心痛点。通过智能分类、标签推荐和跨设备同步，提供现代化的书签管理体验。

### 1.2 核心价值主张

| 价值 | 描述 |
|------|------|
| 🧠 **智能分类** | AI 自动为书签推荐分类和标签 |
| 🔍 **快速检索** | 语义搜索 + 全文搜索，秒级定位 |
| 🔗 **链接健康** | 自动检测失效链接，保持书签有效 |
| 📱 **跨设备同步** | 多浏览器、多设备无缝同步 |
| 🔒 **隐私优先** | 本地优先存储，可选端到端加密 |

### 1.3 目标用户

- **重度浏览器用户**: 收藏大量网页，需要高效管理
- **知识工作者**: 需要组织研究资料和参考链接
- **开发者**: 管理技术文档、API 参考和工具链接
- **学生**: 组织学习资料和课程资源

---

## 2. 系统架构

### 2.1 高层架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           智能书签插件架构                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                        用户界面层 (UI Layer)                      │    │
│  │  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────────────┐     │    │
│  │  │  Popup  │  │ Sidepanel│  │ Options │  │ Context Menu    │     │    │
│  │  │  弹窗   │  │  侧边栏  │  │ 设置页  │  │  右键菜单       │     │    │
│  │  └────┬────┘  └────┬────┘  └────┬────┘  └────────┬────────┘     │    │
│  └───────┼────────────┼────────────┼────────────────┼──────────────┘    │
│          │            │            │                │                    │
│          ▼            ▼            ▼                ▼                    │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                     状态管理层 (State Layer)                      │    │
│  │  ┌──────────────────────────────────────────────────────────┐   │    │
│  │  │              Zustand Store (响应式状态管理)                │   │    │
│  │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │   │    │
│  │  │  │ Bookmark │  │  Folder  │  │ Settings │  │   UI     │  │   │    │
│  │  │  │  Store   │  │  Store   │  │  Store   │  │  Store   │  │   │    │
│  │  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │   │    │
│  │  └──────────────────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                    │                                     │
│                                    ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                      服务层 (Service Layer)                       │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │    │
│  │  │ Bookmark │  │    AI    │  │   Sync   │  │   Link Health    │ │    │
│  │  │ Service  │  │ Service  │  │ Service  │  │    Service       │ │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘ │    │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────────┐ │    │
│  │  │ Storage  │  │  Search  │  │  Export  │  │   Analytics      │ │    │
│  │  │ Service  │  │ Service  │  │ Service  │  │   Service        │ │    │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                    │                                     │
│                                    ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                   后台服务层 (Background Layer)                   │    │
│  │  ┌──────────────────────────────────────────────────────────┐   │    │
│  │  │              Service Worker (Manifest V3)                  │   │    │
│  │  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │   │    │
│  │  │  │ Message  │  │  Alarm   │  │  Event   │  │  State   │  │   │    │
│  │  │  │ Handler  │  │ Manager  │  │ Listener │  │ Persist  │  │   │    │
│  │  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │   │    │
│  │  └──────────────────────────────────────────────────────────┘   │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                    │                                     │
│                                    ▼                                     │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                      数据层 (Data Layer)                          │    │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────────────┐ │    │
│  │  │   IndexedDB    │  │ chrome.storage │  │   Cloud Sync       │ │    │
│  │  │  (主数据存储)   │  │  (配置/缓存)    │  │  (可选远程同步)     │ │    │
│  │  └────────────────┘  └────────────────┘  └────────────────────┘ │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 组件交互流程

```
┌──────────────────────────────────────────────────────────────────────────┐
│                           核心交互流程                                    │
└──────────────────────────────────────────────────────────────────────────┘

用户添加书签流程:
┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌─────────┐
│  用户   │────▶│  Popup   │────▶│ Service  │────▶│    AI    │────▶│ IndexDB │
│ 点击添加 │     │  UI      │     │ Worker   │     │ Classify │     │  保存   │
└─────────┘     └──────────┘     └──────────┘     └──────────┘     └─────────┘
                                       │                                 │
                                       │          ┌──────────┐           │
                                       └─────────▶│  Sync    │◀──────────┘
                                                  │ Service  │
                                                  └──────────┘
                                                       │
                                                       ▼
                                                ┌──────────┐
                                                │  Cloud   │
                                                │ (可选)   │
                                                └──────────┘

搜索书签流程:
┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  用户   │────▶│  Search  │────▶│  Search  │────▶│ IndexDB  │
│ 输入关键词│     │   Bar    │     │ Service  │     │  查询    │
└─────────┘     └──────────┘     └──────────┘     └──────────┘
                                       │
                                       ▼
                                 ┌──────────┐
                                 │ 结果排序  │
                                 │ & 渲染   │
                                 └──────────┘
```

---

## 3. 技术栈选型

### 3.1 核心技术栈

| 层级 | 技术选型 | 理由 |
|------|---------|------|
| **框架** | WXT | Manifest V3 原生支持，优秀开发体验 |
| **前端** | React 18 + TypeScript | 成熟生态，类型安全 |
| **状态管理** | Zustand | 轻量级，简洁 API |
| **UI 组件** | shadcn/ui + Tailwind CSS | 现代设计，高度可定制 |
| **本地存储** | IndexedDB (Dexie.js) | 大容量，结构化查询 |
| **AI/NLP** | Natural + TensorFlow.js (可选) | 本地推理，隐私保护 |
| **云同步** | Supabase (可选) | 开源，实时同步 |

### 3.2 依赖包清单

```json
{
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "zustand": "^4.5.0",
    "dexie": "^4.0.0",
    "natural": "^7.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "fuse.js": "^7.0.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.2.0"
  },
  "devDependencies": {
    "wxt": "^0.19.0",
    "typescript": "^5.4.0",
    "@types/react": "^18.3.0",
    "@types/chrome": "^0.0.270",
    "tailwindcss": "^3.4.0",
    "vitest": "^2.0.0",
    "playwright": "^1.45.0"
  }
}
```

---

## 4. 数据模型设计

### 4.1 核心实体关系图 (ERD)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            数据模型关系图                                 │
└─────────────────────────────────────────────────────────────────────────┘

                    ┌───────────────────────┐
                    │       Folder          │
                    │───────────────────────│
                    │ id: string (PK)       │
                    │ name: string          │
                    │ icon: string          │
                    │ color: string         │
                    │ parentId: string (FK) │◄────┐ (自引用)
                    │ order: number         │     │
                    │ isSmartFolder: bool   │─────┘
                    │ smartFilters: object  │
                    │ createdAt: timestamp  │
                    │ updatedAt: timestamp  │
                    └───────────┬───────────┘
                                │
                                │ 1:N
                                ▼
┌───────────────────────────────────────────────────────────────┐
│                          Bookmark                              │
│───────────────────────────────────────────────────────────────│
│ id: string (PK)                                                │
│ url: string (UNIQUE, INDEX)                                    │
│ title: string (INDEX)                                          │
│ description: string                                            │
│ folderId: string (FK) ─────────────────────────────────────────┤
│ favicon: string                                                │
│ screenshot: string                                             │
│ createdAt: timestamp (INDEX)                                   │
│ updatedAt: timestamp                                           │
│ lastVisited: timestamp                                         │
│ visitCount: number                                             │
│ isFavorite: boolean (INDEX)                                    │
│ isArchived: boolean                                            │
│ status: enum('active', 'broken', 'pending')                    │
│ notes: string                                                  │
│ aiGenerated: boolean                                           │
│ meta: object                                                   │
└───────────────────────────────────────────────────────────────┘
        │                                           │
        │ M:N                                       │ 1:N
        ▼                                           ▼
┌───────────────────┐                   ┌───────────────────────┐
│       Tag         │                   │     LinkCheck         │
│───────────────────│                   │───────────────────────│
│ id: string (PK)   │                   │ id: string (PK)       │
│ name: string (UQ) │                   │ bookmarkId: string(FK)│
│ color: string     │                   │ status: number        │
│ usageCount: number│                   │ checkedAt: timestamp  │
│ createdAt: time   │                   │ responseTime: number  │
└───────────────────┘                   │ errorMessage: string  │
        │                               └───────────────────────┘
        │ M:N (关联表)
        ▼
┌───────────────────────┐
│   BookmarkTag         │
│───────────────────────│
│ bookmarkId: string(FK)│
│ tagId: string (FK)    │
│ (复合主键)             │
└───────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│                         Settings                               │
│───────────────────────────────────────────────────────────────│
│ key: string (PK)                                               │
│ value: any                                                     │
│ updatedAt: timestamp                                           │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│                        SyncMeta                                │
│───────────────────────────────────────────────────────────────│
│ id: string (PK)                                                │
│ entityType: string                                             │
│ entityId: string                                               │
│ version: number                                                │
│ lastSyncedAt: timestamp                                        │
│ syncStatus: enum('synced', 'pending', 'conflict')              │
└───────────────────────────────────────────────────────────────┘
```

### 4.2 TypeScript 类型定义

```typescript
// types/bookmark.ts

export interface Bookmark {
  id: string;
  url: string;
  title: string;
  description?: string;
  folderId?: string;
  tags: string[];
  favicon?: string;
  screenshot?: string;
  createdAt: number;
  updatedAt: number;
  lastVisited?: number;
  visitCount: number;
  isFavorite: boolean;
  isArchived: boolean;
  status: BookmarkStatus;
  notes?: string;
  aiGenerated: boolean;
  meta?: BookmarkMeta;
}

export type BookmarkStatus = 'active' | 'broken' | 'pending';

export interface BookmarkMeta {
  author?: string;
  publishDate?: string;
  readingTime?: string;
  language?: string;
  contentType?: ContentType;
}

export type ContentType =
  | 'article'
  | 'video'
  | 'documentation'
  | 'tool'
  | 'social'
  | 'shopping'
  | 'other';

// types/folder.ts

export interface Folder {
  id: string;
  name: string;
  icon?: string;
  color?: string;
  parentId?: string;
  order: number;
  isSmartFolder: boolean;
  smartFilters?: SmartFilters;
  createdAt: number;
  updatedAt: number;
}

export interface SmartFilters {
  tags?: string[];
  dateRange?: { start: number; end: number };
  status?: BookmarkStatus[];
  contentTypes?: ContentType[];
  isFavorite?: boolean;
}

// types/tag.ts

export interface Tag {
  id: string;
  name: string;
  color?: string;
  usageCount: number;
  createdAt: number;
}

// types/settings.ts

export interface Settings {
  // 通用设置
  theme: 'light' | 'dark' | 'system';
  language: string;

  // AI 设置
  aiEnabled: boolean;
  aiProvider: 'local' | 'openai' | 'claude';
  aiApiKey?: string;
  autoClassify: boolean;
  autoTagSuggestion: boolean;

  // 同步设置
  syncEnabled: boolean;
  syncProvider?: 'supabase' | 'custom';
  syncInterval: number; // 分钟

  // 链接健康设置
  linkCheckEnabled: boolean;
  linkCheckInterval: number; // 小时

  // 导入导出设置
  exportFormat: 'json' | 'html';

  // 界面设置
  defaultView: 'list' | 'card' | 'compact';
  showFavicons: boolean;
  showScreenshots: boolean;
  itemsPerPage: number;
}
```

### 4.3 IndexedDB Schema (Dexie.js)

```typescript
// services/database.ts

import Dexie, { Table } from 'dexie';
import type { Bookmark, Folder, Tag, Settings, LinkCheck, SyncMeta } from '@/types';

export class BookmarkDatabase extends Dexie {
  bookmarks!: Table<Bookmark>;
  folders!: Table<Folder>;
  tags!: Table<Tag>;
  bookmarkTags!: Table<{ bookmarkId: string; tagId: string }>;
  linkChecks!: Table<LinkCheck>;
  settings!: Table<{ key: string; value: any }>;
  syncMeta!: Table<SyncMeta>;

  constructor() {
    super('SmartBookmarkDB');

    this.version(1).stores({
      bookmarks: 'id, url, title, folderId, createdAt, isFavorite, status, [folderId+createdAt]',
      folders: 'id, name, parentId, order, [parentId+order]',
      tags: 'id, &name, usageCount',
      bookmarkTags: '[bookmarkId+tagId], bookmarkId, tagId',
      linkChecks: 'id, bookmarkId, checkedAt',
      settings: 'key',
      syncMeta: 'id, [entityType+entityId], syncStatus'
    });
  }
}

export const db = new BookmarkDatabase();
```

---

## 5. 模块设计

### 5.1 模块依赖图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           模块依赖关系图                                  │
└─────────────────────────────────────────────────────────────────────────┘

                          ┌─────────────────┐
                          │     UI Layer    │
                          │  (React 组件)   │
                          └────────┬────────┘
                                   │
                    ┌──────────────┼──────────────┐
                    ▼              ▼              ▼
           ┌───────────────┐ ┌─────────┐ ┌───────────────┐
           │    Hooks      │ │ Stores  │ │  Components   │
           │ (自定义 Hook) │ │(Zustand)│ │  (UI 组件)    │
           └───────┬───────┘ └────┬────┘ └───────────────┘
                   │              │
                   └──────┬───────┘
                          ▼
              ┌───────────────────────┐
              │    Service Layer      │
              │    (业务逻辑层)        │
              └───────────┬───────────┘
                          │
     ┌────────────────────┼────────────────────┐
     ▼                    ▼                    ▼
┌──────────┐       ┌──────────┐         ┌──────────┐
│ Bookmark │       │    AI    │         │   Sync   │
│ Service  │       │ Service  │         │ Service  │
└────┬─────┘       └────┬─────┘         └────┬─────┘
     │                  │                    │
     │    ┌─────────────┼─────────────┐      │
     │    ▼             ▼             ▼      │
     │ ┌──────────┐ ┌──────────┐ ┌──────────┐│
     │ │ Storage  │ │  Search  │ │  Export  ││
     │ │ Service  │ │ Service  │ │ Service  ││
     │ └────┬─────┘ └────┬─────┘ └────┬─────┘│
     │      │            │            │      │
     └──────┼────────────┼────────────┼──────┘
            │            │            │
            ▼            ▼            ▼
     ┌─────────────────────────────────────────┐
     │              Data Layer                  │
     │  ┌──────────┐  ┌────────┐  ┌──────────┐ │
     │  │ IndexedDB│  │Storage │  │  Cloud   │ │
     │  │ (Dexie)  │  │  API   │  │  API     │ │
     │  └──────────┘  └────────┘  └──────────┘ │
     └─────────────────────────────────────────┘
```

### 5.2 核心服务接口

#### BookmarkService

```typescript
// services/bookmarkService.ts

export interface IBookmarkService {
  // CRUD 操作
  create(bookmark: CreateBookmarkDTO): Promise<Bookmark>;
  update(id: string, data: UpdateBookmarkDTO): Promise<Bookmark>;
  delete(id: string): Promise<void>;
  deleteMany(ids: string[]): Promise<void>;
  getById(id: string): Promise<Bookmark | undefined>;
  getAll(options?: QueryOptions): Promise<Bookmark[]>;

  // 批量操作
  importFromBrowser(): Promise<ImportResult>;
  importFromFile(file: File): Promise<ImportResult>;
  exportToFile(format: 'json' | 'html'): Promise<Blob>;

  // 分类和标签
  moveToFolder(bookmarkIds: string[], folderId: string): Promise<void>;
  addTags(bookmarkId: string, tags: string[]): Promise<void>;
  removeTags(bookmarkId: string, tags: string[]): Promise<void>;

  // 收藏和归档
  toggleFavorite(id: string): Promise<void>;
  archive(ids: string[]): Promise<void>;
  unarchive(ids: string[]): Promise<void>;

  // 重复检测
  findDuplicates(): Promise<DuplicateGroup[]>;
  mergeDuplicates(bookmarkIds: string[], keepId: string): Promise<void>;
}
```

#### AIService

```typescript
// services/aiService.ts

export interface IAIService {
  // 分类和标签
  classifyBookmark(bookmark: Bookmark): Promise<ClassificationResult>;
  suggestTags(bookmark: Bookmark): Promise<string[]>;
  suggestFolder(bookmark: Bookmark): Promise<string | null>;

  // 批量处理
  batchClassify(bookmarks: Bookmark[]): Promise<ClassificationResult[]>;

  // 内容分析
  extractKeywords(content: string): Promise<string[]>;
  summarize(content: string): Promise<string>;
  detectContentType(url: string, title: string): Promise<ContentType>;

  // 配置
  setProvider(provider: AIProvider): void;
  setApiKey(apiKey: string): void;
}

export interface ClassificationResult {
  suggestedFolder?: string;
  suggestedTags: string[];
  contentType: ContentType;
  confidence: number;
}
```

#### SearchService

```typescript
// services/searchService.ts

export interface ISearchService {
  // 基础搜索
  search(query: string, options?: SearchOptions): Promise<SearchResult>;

  // 高级搜索
  advancedSearch(filters: SearchFilters): Promise<SearchResult>;

  // 全文搜索
  fullTextSearch(query: string): Promise<SearchResult>;

  // 索引管理
  rebuildIndex(): Promise<void>;
  updateIndex(bookmark: Bookmark): Promise<void>;
  removeFromIndex(bookmarkId: string): Promise<void>;
}

export interface SearchOptions {
  limit?: number;
  offset?: number;
  sortBy?: 'relevance' | 'date' | 'title' | 'visitCount';
  sortOrder?: 'asc' | 'desc';
}

export interface SearchFilters {
  query?: string;
  folders?: string[];
  tags?: string[];
  dateRange?: { start: Date; end: Date };
  status?: BookmarkStatus[];
  isFavorite?: boolean;
  isArchived?: boolean;
  contentTypes?: ContentType[];
}
```

#### SyncService

```typescript
// services/syncService.ts

export interface ISyncService {
  // 同步控制
  sync(): Promise<SyncResult>;
  startAutoSync(intervalMinutes: number): void;
  stopAutoSync(): void;

  // 状态查询
  getSyncStatus(): Promise<SyncStatus>;
  getLastSyncTime(): Promise<Date | null>;
  getPendingChanges(): Promise<number>;

  // 冲突处理
  getConflicts(): Promise<SyncConflict[]>;
  resolveConflict(conflictId: string, resolution: 'local' | 'remote' | 'merge'): Promise<void>;

  // 配置
  configure(config: SyncConfig): void;
}

export interface SyncResult {
  success: boolean;
  uploaded: number;
  downloaded: number;
  conflicts: number;
  errors: string[];
}
```

#### LinkHealthService

```typescript
// services/linkHealthService.ts

export interface ILinkHealthService {
  // 链接检查
  checkLink(url: string): Promise<LinkCheckResult>;
  checkBookmark(bookmarkId: string): Promise<LinkCheckResult>;
  checkAllBookmarks(): Promise<LinkHealthReport>;

  // 定时检查
  startScheduledCheck(intervalHours: number): void;
  stopScheduledCheck(): void;

  // 报告
  getHealthReport(): Promise<LinkHealthReport>;
  getBrokenLinks(): Promise<Bookmark[]>;

  // 修复
  markAsFixed(bookmarkId: string): Promise<void>;
  updateUrl(bookmarkId: string, newUrl: string): Promise<void>;
}

export interface LinkCheckResult {
  url: string;
  status: number;
  isAccessible: boolean;
  responseTime: number;
  errorMessage?: string;
  checkedAt: Date;
}
```

---

## 6. UI 组件架构

### 6.1 组件层次结构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          UI 组件层次结构                                  │
└─────────────────────────────────────────────────────────────────────────┘

App
├── Layout
│   ├── Header
│   │   ├── Logo
│   │   ├── SearchBar
│   │   └── QuickActions
│   ├── Sidebar (可折叠)
│   │   ├── FolderTree
│   │   ├── TagCloud
│   │   └── SmartFilters
│   └── MainContent
│       ├── Toolbar
│       │   ├── ViewToggle (list/card/compact)
│       │   ├── SortDropdown
│       │   ├── FilterDropdown
│       │   └── BatchActions
│       ├── BookmarkList / BookmarkGrid
│       │   ├── BookmarkCard
│       │   │   ├── Favicon
│       │   │   ├── Title
│       │   │   ├── Description
│       │   │   ├── Tags
│       │   │   └── Actions (edit, delete, favorite)
│       │   └── EmptyState
│       └── Pagination
│
├── Dialogs
│   ├── AddBookmarkDialog
│   ├── EditBookmarkDialog
│   ├── FolderManageDialog
│   ├── TagManageDialog
│   ├── ImportDialog
│   ├── ExportDialog
│   └── SettingsDialog
│
├── Shared Components
│   ├── Button
│   ├── Input
│   ├── Select
│   ├── Checkbox
│   ├── Modal
│   ├── Toast
│   ├── Tooltip
│   ├── Dropdown
│   ├── ContextMenu
│   ├── Skeleton
│   └── ErrorBoundary
│
└── Providers
    ├── ThemeProvider
    ├── ToastProvider
    └── QueryProvider
```

### 6.2 页面结构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           Popup 页面布局                                  │
├─────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │ Header                                                              │ │
│ │ ┌──────┐ ┌────────────────────────────────────┐ ┌─────┐ ┌─────┐    │ │
│ │ │ Logo │ │ 🔍 Search bookmarks...              │ │ ⚙️  │ │ ➕  │    │ │
│ │ └──────┘ └────────────────────────────────────┘ └─────┘ └─────┘    │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │ Quick Actions                                                        │ │
│ │ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐             │ │
│ │ │ 📌 收藏    │ │ 📁 最近   │ │ ❤️ 喜欢   │ │ 🔗 失效   │             │ │
│ │ └───────────┘ └───────────┘ └───────────┘ └───────────┘             │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │ Bookmark List                                                        │ │
│ │ ┌───────────────────────────────────────────────────────────────┐   │ │
│ │ │ 🌐 Example Website                                         ⋮ │   │ │
│ │ │    https://example.com                                        │   │ │
│ │ │    [技术] [教程]                                 ❤️ 2天前    │   │ │
│ │ └───────────────────────────────────────────────────────────────┘   │ │
│ │ ┌───────────────────────────────────────────────────────────────┐   │ │
│ │ │ 📄 Documentation Page                                      ⋮ │   │ │
│ │ │    https://docs.example.com                                   │   │ │
│ │ │    [文档] [API]                                  🤍 1周前    │   │ │
│ │ └───────────────────────────────────────────────────────────────┘   │ │
│ │ ...                                                                  │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
│ ┌─────────────────────────────────────────────────────────────────────┐ │
│ │ Footer                                                              │ │
│ │ 显示 10 / 256 书签                            [打开管理器]          │ │
│ └─────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                         Sidepanel 侧边栏布局                              │
├─────────────────────────────────────────────────────────────────────────┤
│ ┌────────────┬──────────────────────────────────────────────────────┐  │
│ │  文件夹     │  ┌──────────────────────────────────────────────┐   │  │
│ │  ─────────  │  │ 🔍 Search...                     [📥] [📤]  │   │  │
│ │  📁 全部    │  └──────────────────────────────────────────────┘   │  │
│ │  📁 工作    │  ┌──────────────────────────────────────────────┐   │  │
│ │    └─ 开发  │  │ View: [列表 ▼]  Sort: [日期 ▼]  Filter: [全部]│   │  │
│ │    └─ 设计  │  └──────────────────────────────────────────────┘   │  │
│ │  📁 学习    │  ┌──────────────────────────────────────────────┐   │  │
│ │  📁 娱乐    │  │  书签列表区域                                 │   │  │
│ │  ─────────  │  │  ┌────────────────────────────────────────┐  │   │  │
│ │  标签      │  │  │ 🌐 Example Site                     ⋮ │  │   │  │
│ │  ─────────  │  │  │    https://example.com               │  │   │  │
│ │  [技术] 45  │  │  │    [技术] [教程]       ❤️ 2天前      │  │   │  │
│ │  [教程] 32  │  │  └────────────────────────────────────────┘  │   │  │
│ │  [工具] 28  │  │  ...                                         │   │  │
│ │  [文档] 21  │  └──────────────────────────────────────────────┘   │  │
│ │             │                                                      │  │
│ └────────────┴──────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 7. 消息通信架构

### 7.1 消息类型定义

```typescript
// types/messages.ts

export type MessageType =
  // 书签操作
  | 'BOOKMARK_CREATE'
  | 'BOOKMARK_UPDATE'
  | 'BOOKMARK_DELETE'
  | 'BOOKMARK_BATCH_DELETE'
  | 'BOOKMARK_IMPORT'
  | 'BOOKMARK_EXPORT'

  // AI 操作
  | 'AI_CLASSIFY'
  | 'AI_SUGGEST_TAGS'
  | 'AI_BATCH_CLASSIFY'

  // 同步操作
  | 'SYNC_START'
  | 'SYNC_STATUS'
  | 'SYNC_COMPLETE'

  // 链接检查
  | 'LINK_CHECK_START'
  | 'LINK_CHECK_RESULT'
  | 'LINK_CHECK_COMPLETE'

  // 通用
  | 'GET_STATE'
  | 'UPDATE_SETTINGS'
  | 'ERROR';

export interface Message<T = any> {
  type: MessageType;
  payload?: T;
  requestId?: string;
}

export interface MessageResponse<T = any> {
  success: boolean;
  data?: T;
  error?: string;
  requestId?: string;
}
```

### 7.2 消息流转图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          消息通信流转图                                   │
└─────────────────────────────────────────────────────────────────────────┘

┌───────────────┐         ┌───────────────┐         ┌───────────────┐
│    Popup      │         │  Service      │         │   Content     │
│    (UI)       │         │  Worker       │         │   Script      │
└───────┬───────┘         └───────┬───────┘         └───────┬───────┘
        │                         │                         │
        │  BOOKMARK_CREATE        │                         │
        │────────────────────────▶│                         │
        │                         │                         │
        │                         │  (处理业务逻辑)          │
        │                         │  - 验证数据              │
        │                         │  - AI 分类              │
        │                         │  - 保存到 IndexedDB     │
        │                         │                         │
        │  Response (success)     │                         │
        │◀────────────────────────│                         │
        │                         │                         │
        │                         │  SYNC_START             │
        │                         │─────────────────────────│
        │                         │  (后台同步)              │
        │                         │                         │
        │                         │                         │
        │                         │  GET_PAGE_INFO          │
        │                         │────────────────────────▶│
        │                         │                         │
        │                         │  Page metadata          │
        │                         │◀────────────────────────│
        │                         │                         │
        ▼                         ▼                         ▼

消息处理流程:

1. UI 层发送消息 ─────────────────────────────────────────────────────────▶
   chrome.runtime.sendMessage({ type: 'BOOKMARK_CREATE', payload: {...} })

2. Service Worker 接收并处理 ────────────────────────────────────────────▶
   chrome.runtime.onMessage.addListener((message, sender, sendResponse) => {
     handleMessage(message).then(sendResponse);
     return true; // 异步响应
   });

3. Service Worker 发送广播 (状态更新) ───────────────────────────────────▶
   chrome.runtime.sendMessage({ type: 'STATE_UPDATED', payload: {...} })

4. 所有监听者接收更新 ───────────────────────────────────────────────────▶
   各 UI 组件更新状态
```

---

## 8. 安全架构

### 8.1 安全模型

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            安全架构设计                                   │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                          Content Security Policy                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ "content_security_policy": {                                     │   │
│  │   "extension_pages": "script-src 'self'; object-src 'self'"     │   │
│  │ }                                                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                           权限最小化原则                                  │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │ 必要权限:                                                        │   │
│  │   - bookmarks: 读写浏览器书签                                    │   │
│  │   - storage: 存储扩展数据                                        │   │
│  │   - tabs: 获取当前标签页信息                                     │   │
│  │   - activeTab: 获取活动标签页内容                                │   │
│  │   - alarms: 定时任务（链接检查）                                 │   │
│  │                                                                   │   │
│  │ 可选权限 (运行时请求):                                           │   │
│  │   - host_permissions: 抓取网页内容进行分析                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────┐
│                            数据加密方案                                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                                                                   │   │
│  │  本地存储 (默认):                                                │   │
│  │  ┌─────────────┐                                                 │   │
│  │  │  IndexedDB  │ ──▶ 明文存储 (浏览器沙箱保护)                   │   │
│  │  └─────────────┘                                                 │   │
│  │                                                                   │   │
│  │  云端同步 (可选):                                                │   │
│  │  ┌─────────────┐     ┌─────────────┐     ┌─────────────┐        │   │
│  │  │  本地数据   │────▶│  AES-256    │────▶│  云端存储   │        │   │
│  │  │            │     │  加密       │     │  (密文)     │        │   │
│  │  └─────────────┘     └─────────────┘     └─────────────┘        │   │
│  │                            │                                      │   │
│  │                            ▼                                      │   │
│  │                    ┌─────────────┐                               │   │
│  │                    │  用户密钥   │ (仅存储于本地)                 │   │
│  │                    └─────────────┘                               │   │
│  │                                                                   │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

### 8.2 Manifest 权限配置

```json
{
  "manifest_version": 3,
  "name": "智能书签",
  "version": "1.0.0",

  "permissions": [
    "bookmarks",
    "storage",
    "tabs",
    "activeTab",
    "alarms",
    "contextMenus"
  ],

  "optional_host_permissions": [
    "https://*/*",
    "http://*/*"
  ],

  "content_security_policy": {
    "extension_pages": "script-src 'self'; object-src 'self'"
  },

  "background": {
    "service_worker": "background/index.js",
    "type": "module"
  },

  "action": {
    "default_popup": "popup/index.html",
    "default_icon": {
      "16": "icons/icon16.png",
      "48": "icons/icon48.png",
      "128": "icons/icon128.png"
    }
  },

  "side_panel": {
    "default_path": "sidepanel/index.html"
  },

  "options_page": "options/index.html",

  "icons": {
    "16": "icons/icon16.png",
    "48": "icons/icon48.png",
    "128": "icons/icon128.png"
  }
}
```

---

## 9. 性能优化策略

### 9.1 性能目标

| 指标 | 目标值 | 测量方法 |
|------|--------|---------|
| 扩展加载时间 | < 500ms | Performance API |
| 搜索响应时间 | < 200ms | 用户操作到结果渲染 |
| AI 分类时间 | < 1s (本地) | 单个书签分类 |
| 内存使用 | < 50MB (空闲) | Chrome Task Manager |
| IndexedDB 查询 | < 100ms | 1万条记录场景 |

### 9.2 优化策略

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           性能优化策略                                    │
└─────────────────────────────────────────────────────────────────────────┘

1. 数据加载优化
   ┌─────────────────────────────────────────────────────────────────┐
   │  初始加载                                                        │
   │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐       │
   │  │ 骨架屏显示   │───▶│ 首屏数据加载 │───▶│ 懒加载剩余   │       │
   │  │ (即时)       │    │ (前20条)     │    │ (按需)       │       │
   │  └──────────────┘    └──────────────┘    └──────────────┘       │
   └─────────────────────────────────────────────────────────────────┘

2. 列表渲染优化
   ┌─────────────────────────────────────────────────────────────────┐
   │  虚拟化列表 (react-virtual)                                      │
   │  ┌──────────────────────────────────────────────────────────┐   │
   │  │  可视区域 (Viewport)                                      │   │
   │  │  ┌────────────────────────────────────────────────────┐  │   │
   │  │  │  只渲染可见项 + 缓冲区                              │  │   │
   │  │  │  (约 20-30 个 DOM 节点)                            │  │   │
   │  │  └────────────────────────────────────────────────────┘  │   │
   │  │  ═══════════════════════════════════════════════════════  │   │
   │  │  虚拟区域 (占位)                                          │   │
   │  │  (无 DOM 节点，仅 CSS 高度)                               │   │
   │  └──────────────────────────────────────────────────────────┘   │
   └─────────────────────────────────────────────────────────────────┘

3. 搜索优化
   ┌─────────────────────────────────────────────────────────────────┐
   │  搜索流程                                                        │
   │  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐  │
   │  │ 用户输入 │───▶│ 防抖     │───▶│ Fuse.js  │───▶│ 结果渲染 │  │
   │  │          │    │ (300ms)  │    │ 模糊搜索 │    │          │  │
   │  └──────────┘    └──────────┘    └──────────┘    └──────────┘  │
   │                                        │                        │
   │                                        ▼                        │
   │                                 ┌──────────┐                    │
   │                                 │ 预计算索引│                    │
   │                                 │ (启动时) │                    │
   │                                 └──────────┘                    │
   └─────────────────────────────────────────────────────────────────┘

4. Service Worker 状态管理
   ┌─────────────────────────────────────────────────────────────────┐
   │  状态持久化 (应对 SW 终止)                                       │
   │  ┌──────────┐    ┌──────────┐    ┌──────────┐                  │
   │  │ SW 活跃  │───▶│ 定期保存 │───▶│ 重启恢复 │                  │
   │  │          │    │ (5分钟)  │    │          │                  │
   │  └──────────┘    └──────────┘    └──────────┘                  │
   │                                                                  │
   │  使用 chrome.storage.session 存储临时状态                       │
   │  使用 chrome.storage.local 存储持久状态                         │
   └─────────────────────────────────────────────────────────────────┘

5. 图片优化
   ┌─────────────────────────────────────────────────────────────────┐
   │  Favicon 和截图处理                                              │
   │  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐      │
   │  │ 原始图片     │───▶│ 压缩/缩放   │───▶│ Base64 存储  │      │
   │  │              │    │ (WebP)      │    │ (IndexedDB)  │      │
   │  └──────────────┘    └──────────────┘    └──────────────┘      │
   │                                                                  │
   │  - Favicon: 16x16 或 32x32                                      │
   │  - 截图: 320x180 (16:9)                                         │
   │  - 格式: WebP (体积小)                                          │
   │  - 延迟加载: Intersection Observer                              │
   └─────────────────────────────────────────────────────────────────┘
```

---

## 10. 错误处理架构

### 10.1 错误分类

```typescript
// types/errors.ts

export enum ErrorCode {
  // 存储错误 (1xxx)
  STORAGE_QUOTA_EXCEEDED = 1001,
  STORAGE_READ_ERROR = 1002,
  STORAGE_WRITE_ERROR = 1003,

  // 网络错误 (2xxx)
  NETWORK_OFFLINE = 2001,
  NETWORK_TIMEOUT = 2002,
  NETWORK_REQUEST_FAILED = 2003,

  // AI 错误 (3xxx)
  AI_SERVICE_UNAVAILABLE = 3001,
  AI_RATE_LIMITED = 3002,
  AI_INVALID_RESPONSE = 3003,

  // 同步错误 (4xxx)
  SYNC_CONFLICT = 4001,
  SYNC_AUTH_FAILED = 4002,
  SYNC_SERVER_ERROR = 4003,

  // 数据错误 (5xxx)
  DATA_VALIDATION_FAILED = 5001,
  DATA_NOT_FOUND = 5002,
  DATA_DUPLICATE = 5003,

  // 未知错误
  UNKNOWN_ERROR = 9999
}

export class AppError extends Error {
  constructor(
    public code: ErrorCode,
    message: string,
    public details?: any
  ) {
    super(message);
    this.name = 'AppError';
  }
}
```

### 10.2 错误处理流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          错误处理流程                                     │
└─────────────────────────────────────────────────────────────────────────┘

错误发生
    │
    ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│  错误捕获   │────▶│  错误分类   │────▶│  处理策略   │
│  try/catch  │     │  ErrorCode  │     │             │
└─────────────┘     └─────────────┘     └──────┬──────┘
                                               │
                    ┌──────────────────────────┼──────────────────────────┐
                    │                          │                          │
                    ▼                          ▼                          ▼
           ┌─────────────┐            ┌─────────────┐            ┌─────────────┐
           │   可恢复    │            │  需用户操作  │            │   致命错误   │
           │   自动重试   │            │  显示提示   │            │   上报日志   │
           └─────────────┘            └─────────────┘            └─────────────┘
                    │                          │                          │
                    ▼                          ▼                          ▼
           ┌─────────────┐            ┌─────────────┐            ┌─────────────┐
           │ 指数退避重试 │            │ Toast 提示  │            │ 错误边界    │
           │ 最多 3 次   │            │ 用户确认    │            │ 降级 UI     │
           └─────────────┘            └─────────────┘            └─────────────┘

用户友好错误消息:
┌─────────────────────────────────────────────────────────────────────────┐
│  ErrorCode → 用户消息映射                                               │
│                                                                          │
│  STORAGE_QUOTA_EXCEEDED  → "存储空间不足，请清理部分书签"                │
│  NETWORK_OFFLINE         → "网络连接已断开，请检查网络设置"              │
│  AI_RATE_LIMITED         → "AI 服务繁忙，请稍后重试"                    │
│  SYNC_CONFLICT           → "同步冲突，请选择保留的版本"                  │
│  DATA_DUPLICATE          → "该书签已存在"                               │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 11. 测试策略

### 11.1 测试金字塔

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            测试金字塔                                    │
└─────────────────────────────────────────────────────────────────────────┘

                        ┌───────────┐
                       ╱             ╲
                      ╱   E2E 测试    ╲         5%
                     ╱   (Playwright)  ╲
                    ╱───────────────────╲
                   ╱                     ╲
                  ╱     集成测试          ╲       20%
                 ╱    (Service + Store)   ╲
                ╱─────────────────────────╲
               ╱                           ╲
              ╱         单元测试            ╲      75%
             ╱        (Vitest)              ╲
            ╱───────────────────────────────╲
           ╱                                 ╲
          ╱           类型检查                ╲
         ╱          (TypeScript)              ╲
        ╱─────────────────────────────────────╲
```

### 11.2 测试覆盖目标

| 模块 | 覆盖率目标 | 重点测试项 |
|------|-----------|-----------|
| Services | 90% | 业务逻辑、边界条件 |
| Stores | 85% | 状态转换、异步操作 |
| Utils | 95% | 纯函数、数据转换 |
| Components | 70% | 交互逻辑、渲染结果 |
| 整体 | 80% | - |

---

## 12. 部署架构

### 12.1 CI/CD 流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          CI/CD 流程图                                    │
└─────────────────────────────────────────────────────────────────────────┘

┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  Push   │────▶│  Lint   │────▶│  Test   │────▶│  Build  │────▶│ Package │
│ /PR     │     │ + Type  │     │ (Unit)  │     │ (Prod)  │     │ (.zip)  │
└─────────┘     └─────────┘     └─────────┘     └─────────┘     └─────────┘
                                                                      │
                    ┌─────────────────────────────────────────────────┘
                    │
                    ▼
             ┌─────────────┐
             │  发布渠道   │
             └──────┬──────┘
                    │
     ┌──────────────┼──────────────┐
     ▼              ▼              ▼
┌─────────┐   ┌─────────┐   ┌─────────┐
│ Chrome  │   │ Firefox │   │  Edge   │
│ Web     │   │ Add-ons │   │ Add-ons │
│ Store   │   │         │   │         │
└─────────┘   └─────────┘   └─────────┘

GitHub Actions 配置:
┌─────────────────────────────────────────────────────────────────────────┐
│  .github/workflows/ci.yml                                               │
│                                                                          │
│  on: [push, pull_request]                                               │
│  jobs:                                                                   │
│    lint:    运行 ESLint + TypeScript 检查                               │
│    test:    运行 Vitest 单元测试                                        │
│    build:   构建生产版本                                                 │
│    e2e:     运行 Playwright E2E 测试 (仅 main 分支)                     │
│    release: 打包并上传到各应用商店 (仅 tag)                              │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 13. 开发路线图

### 13.1 版本规划

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           版本开发路线图                                  │
└─────────────────────────────────────────────────────────────────────────┘

v0.1.0 MVP (Week 1-4)
├── ✅ 项目初始化 (WXT + React + TypeScript)
├── ✅ 基础 UI 框架 (Popup + Sidepanel)
├── ✅ IndexedDB 数据层
├── ✅ 书签 CRUD 操作
├── ✅ 文件夹管理
├── ✅ 基础搜索
└── ✅ 浏览器书签导入

v0.2.0 智能分类 (Week 5-6)
├── ⬜ 关键词提取 (Natural)
├── ⬜ 自动标签推荐
├── ⬜ 分类建议
├── ⬜ 重复检测
└── ⬜ 批量整理

v0.3.0 增强功能 (Week 7-8)
├── ⬜ 链接健康检查
├── ⬜ 网页截图
├── ⬜ 导入/导出功能
├── ⬜ 快捷键支持
└── ⬜ 右键菜单集成

v0.4.0 同步功能 (Week 9-12)
├── ⬜ Supabase 集成
├── ⬜ 用户认证
├── ⬜ 实时同步
├── ⬜ 冲突解决 UI
└── ⬜ 端到端加密

v1.0.0 正式版 (Week 13-14)
├── ⬜ 性能优化
├── ⬜ 完整测试覆盖
├── ⬜ 文档完善
├── ⬜ 多语言支持
└── ⬜ 应用商店发布

v2.0.0 高级功能 (Future)
├── ⬜ 云端 AI API 集成
├── ⬜ 语义搜索
├── ⬜ 智能文件夹
├── ⬜ 团队协作
└── ⬜ 移动端伴侣应用
```

---

## 14. 附录

### 14.1 目录结构

```
smart-bookmark/
├── .github/
│   └── workflows/
│       └── ci.yml
├── public/
│   └── icons/
│       ├── icon16.png
│       ├── icon48.png
│       └── icon128.png
├── src/
│   ├── background/
│   │   ├── index.ts
│   │   ├── messageHandler.ts
│   │   ├── alarmManager.ts
│   │   └── stateManager.ts
│   ├── content/
│   │   ├── index.ts
│   │   └── pageAnalyzer.ts
│   ├── popup/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.html
│   ├── sidepanel/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.html
│   ├── options/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.html
│   ├── components/
│   │   ├── ui/
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   └── ...
│   │   ├── bookmark/
│   │   │   ├── BookmarkCard.tsx
│   │   │   ├── BookmarkList.tsx
│   │   │   └── BookmarkForm.tsx
│   │   ├── folder/
│   │   │   ├── FolderTree.tsx
│   │   │   └── FolderItem.tsx
│   │   ├── search/
│   │   │   └── SearchBar.tsx
│   │   └── layout/
│   │       ├── Header.tsx
│   │       ├── Sidebar.tsx
│   │       └── MainContent.tsx
│   ├── hooks/
│   │   ├── useBookmarks.ts
│   │   ├── useFolders.ts
│   │   ├── useSearch.ts
│   │   └── useSettings.ts
│   ├── stores/
│   │   ├── bookmarkStore.ts
│   │   ├── folderStore.ts
│   │   ├── settingsStore.ts
│   │   └── uiStore.ts
│   ├── services/
│   │   ├── bookmarkService.ts
│   │   ├── aiService.ts
│   │   ├── searchService.ts
│   │   ├── syncService.ts
│   │   ├── storageService.ts
│   │   ├── linkHealthService.ts
│   │   └── exportService.ts
│   ├── lib/
│   │   ├── database.ts
│   │   ├── messaging.ts
│   │   └── utils.ts
│   ├── types/
│   │   ├── bookmark.ts
│   │   ├── folder.ts
│   │   ├── tag.ts
│   │   ├── settings.ts
│   │   ├── messages.ts
│   │   └── errors.ts
│   └── styles/
│       ├── globals.css
│       └── tailwind.css
├── tests/
│   ├── unit/
│   │   ├── services/
│   │   └── stores/
│   ├── integration/
│   └── e2e/
├── .env.example
├── .eslintrc.cjs
├── .prettierrc
├── tailwind.config.ts
├── tsconfig.json
├── vitest.config.ts
├── wxt.config.ts
├── package.json
└── README.md
```

### 14.2 关键配置文件

#### wxt.config.ts

```typescript
import { defineConfig } from 'wxt';

export default defineConfig({
  modules: ['@wxt-dev/module-react'],
  manifest: {
    name: '智能书签',
    description: 'AI 驱动的智能书签管理插件',
    permissions: ['bookmarks', 'storage', 'tabs', 'activeTab', 'alarms', 'contextMenus'],
    optional_host_permissions: ['https://*/*', 'http://*/*'],
  },
});
```

#### tailwind.config.ts

```typescript
import type { Config } from 'tailwindcss';

export default {
  content: ['./src/**/*.{ts,tsx}'],
  darkMode: 'class',
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#eff6ff',
          500: '#3b82f6',
          600: '#2563eb',
          700: '#1d4ed8',
        },
      },
    },
  },
  plugins: [],
} satisfies Config;
```

---

## 15. 总结

本架构设计文档为智能书签插件提供了完整的技术蓝图，涵盖：

1. **系统架构**: 清晰的分层设计，便于维护和扩展
2. **技术选型**: 基于 WXT + React + TypeScript 的现代技术栈
3. **数据模型**: 完整的实体关系和 TypeScript 类型定义
4. **模块设计**: 明确的服务接口和职责划分
5. **UI 组件**: 结构化的组件层次和页面布局
6. **安全架构**: Manifest V3 安全最佳实践
7. **性能优化**: 针对性的优化策略
8. **测试策略**: 完善的测试金字塔
9. **部署架构**: 自动化的 CI/CD 流程
10. **开发路线图**: 分阶段的版本规划

**下一步行动**: 使用 `/sc:implement` 开始 MVP 开发，按照路线图逐步实现各功能模块。

---

**文档版本**: v1.0
**最后更新**: 2025-01-19
**设计师**: Claude (Sonnet 4.5)
