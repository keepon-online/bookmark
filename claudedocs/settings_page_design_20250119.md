# 设置页面独立化设计方案

**创建时间**: 2025-01-19
**版本**: v1.0
**目标**: 将设置功能独立为单独的页面，提升可用性和可维护性

---

## 📋 需求分析

### 当前问题
1. **功能混杂**: 所有设置都在一个页面，功能太多导致页面过长
2. **难以导航**: 不方便快速找到特定设置
3. **调试困难**: 出问题时不容易定位是哪个功能的问题
4. **体验不佳**: 用户需要在长长的页面中滚动查找

### 设计目标
1. **模块化**: 将设置按功能分成独立页面
2. **清晰的导航**: 提供侧边栏或标签页导航
3. **更好的调试**: 每个功能页面独立，便于定位问题
4. **响应式设计**: 在不同屏幕尺寸下都有良好体验

---

## 🏗️ 架构设计

### 页面结构

```
设置页面 (settings.html)
│
├── 侧边导航栏
│   ├── 仪表盘 (Dashboard)
│   ├── 书签管理 (Bookmarks)
│   ├── AI 设置 (AI Settings)
│   ├── 智能整理 (Organizer)
│   ├── 云端同步 (Sync)
│   ├── 数据统计 (Stats)
│   ├── 链接健康 (Health)
│   ├── 高级设置 (Advanced)
│   └── 关于 (About)
│
└── 主内容区
    └── 动态加载对应的设置组件
```

### 文件结构

```
src/entrypoints/settings/
├── settings.html          # 设置页面入口
├── main.tsx               # 设置页面主组件
├── components/
│   ├── SettingsLayout.tsx  # 布局组件（侧边栏+内容区）
│   ├── SidebarNav.tsx      # 侧边栏导航
│   ├── Dashboard.tsx       # 仪表盘页面
│   ├── BookmarksSettings.tsx
│   ├── AISettings.tsx
│   ├── OrganizerSettings.tsx
│   ├── SyncSettings.tsx
│   ├── StatsSettings.tsx
│   ├── HealthSettings.tsx
│   ├── AdvancedSettings.tsx
│   └── AboutSettings.tsx
```

---

## 📐 UI/UX 设计

### 布局方案

#### 方案 A: 侧边栏 + 主内容区 (推荐)

```
┌─────────────────────────────────────────────────┐
│  设置                    [保存] [重置]            │
├────────┬────────────────────────────────────────┤
│        │                                         │
│ 侧边栏  │  主内容区                            │
│        │                                         │
│ 📊 仪表盘│  [当前选中的页面内容]               │
│        │                                         │
│ 📖 书签  │                                         │
│        │                                         │
│ 🤖 AI   │                                         │
│        │                                         │
│ 🗂️ 整理  │                                         │
│        │                                         │
│ ☁️ 同步  │                                         │
│        │                                         │
│ 📊 统计  │                                         │
│        │                                         │
│ 🔍 健康  │                                         │
│        │                                         │
│ ⚙️ 高级  │                                         │
│        │                                         │
│ ℹ️ 关于  │                                         │
│        │                                         │
└────────┴────────────────────────────────────────┘
```

#### 方案 B: 标签页 (Tab) 导航

```
┌─────────────────────────────────────────────────┐
│  仪表盘 | 书签 | AI | 整理 | 同步 | 统计 | ...   │
├─────────────────────────────────────────────────┤
│                                                 │
│  [当前选中的标签页内容]                          │
│                                                 │
└─────────────────────────────────────────────────┘
```

**推荐**: 方案 A (侧边栏) - 更清晰，功能扩展性更好

---

## 🎨 页面详细设计

### 1. 仪表盘 (Dashboard)

**用途**: 总览和快速操作

**内容**:
- 系统状态卡片
  - 总书签数
  - 今日新增
  - AI 分类数
  - 存储使用情况
- 快速操作
  - 添加书签
  - 整理书签
  - 导入/导出
- 最近活动
  - 最近的整理记录
  - 同步状态
- 调试信息
  - 数据库状态
  - 浏览器同步状态
  - 错误日志摘要

### 2. 书签管理 (Bookmarks)

**用途**: 书签的基础管理

**内容**:
- 书签列表 (分页/搜索)
- 批量操作工具栏
  - 选择全部
  - 批量删除
  - 批量移动
  - 批量加标签
- 导入/导出
  - 从浏览器导入
  - 导出到 JSON
- 清空数据

### 3. AI 设置 (AI Settings)

**用途**: AI 相关配置

**内容**:
- DeepSeek 配置
  - API Key
  - 模型选择
  - Temperature
  - Max Tokens
  - 连接测试
  - 成本统计
- 本地 AI 配置
  - 规则管理
  - 自定义规则
  - 置信度设置
- 自动分类设置
  - 新书签自动分类
  - 自动整理计划

### 4. 智能整理 (Organizer)

**用途**: 书签整理功能

**内容**:
- 整理选项
  - 策略选择
  - 置信度阈值
  - 创建文件夹
  - 应用标签
  - 移动书签
- 自动整理配置
  - 启用/禁用
  - 整理频率
  - 整理时间
- 整理预览
- 执行整理按钮
- 整理历史
- 调试面板 (整合 DebugPanel)

### 5. 云端同步 (Sync)

**用途**: 云同步配置

**内容**:
- 同步服务配置
- 账号管理
- 同步计划
- 同步历史
- 冲突解决策略

### 6. 数据统计 (Stats)

**用途**: 数据分析和统计

**内容**:
- 统计图表
  - 书签增长趋势
  - 标签分布
  - 文件夹分布
  - 域名统计
- 时间范围选择
- 导出报表

### 7. 链接健康 (Health)

**用途**: 链接检查

**内容**:
- 健康检查配置
  - 检查频率
  - 超时设置
- 健康报告
  - 总体状态
  - 失效链接列表
  - 可疑链接
- 手动检查按钮

### 8. 高级设置 (Advanced)

**用途**: 高级功能配置

**内容**:
- 数据管理
  - 清理缓存
  - 重建索引
  - 数据库优化
- 开发者选项
  - 调试模式
  - 日志级别
  - 导出日志
- 实验性功能

### 9. 关于 (About)

**用途**: 应用信息和帮助

**内容**:
- 版本信息
- 更新日志
- 使用指南
- 快捷键列表
- 问题反馈
- 开源仓库链接

---

## 🔄 路由设计

### URL 哈希路由

使用 hash 路由实现页面切换：

```
settings.html                    # 仪表盘 (默认)
settings.html#bookmarks         # 书签管理
settings.html#ai                # AI 设置
settings.html#organizer         # 智能整理
settings.html#sync              # 云端同步
settings.html#stats             # 数据统计
settings.html#health            # 链接健康
settings.html#advanced          # 高级设置
settings.html#about             # 关于
```

### 实现方式

```tsx
const [activeTab, setActiveTab] = useState('dashboard');

useEffect(() => {
  const handleHashChange = () => {
    const hash = window.location.hash.slice(1); // 去掉 #
    setActiveTab(hash || 'dashboard');
  };

  window.addEventListener('hashchange', handleHashChange);
  handleHashChange();

  return () => window.removeEventListener('hashchange', handleHashChange);
}, []);
```

---

## 🎯 组件设计

### SettingsLayout 主布局

```tsx
interface SettingsLayoutProps {
  children: React.ReactNode;
}

export function SettingsLayout({ children }: SettingsLayoutProps) {
  return (
    <div className="flex h-screen bg-gray-50">
      {/* 侧边栏 */}
      <SidebarNav />

      {/* 主内容区 */}
      <main className="flex-1 overflow-y-auto">
        <div className="container mx-auto p-6">
          {children}
        </div>
      </main>
    </div>
  );
}
```

### SidebarNav 侧边栏

```tsx
interface NavItem {
  id: string;
  label: string;
  icon: LucideIcon;
  badge?: number;
}

const navItems: NavItem[] = [
  { id: 'dashboard', label: '仪表盘', icon: LayoutDashboard },
  { id: 'bookmarks', label: '书签管理', icon: Bookmark },
  { id: 'ai', label: 'AI 设置', icon: Sparkles },
  { id: 'organizer', label: '智能整理', icon: Wand2 },
  { id: 'sync', label: '云端同步', icon: Cloud },
  { id: 'stats', label: '数据统计', icon: BarChart3 },
  { id: 'health', label: '链接健康', icon: HeartPulse },
  { id: 'advanced', label: '高级设置', icon: Settings },
  { id: 'about', label: '关于', icon: Info },
];

export function SidebarNav() {
  const [activeTab, setActiveTab] = useState('dashboard');

  return (
    <nav className="w-64 bg-white border-r border-gray-200 p-4">
      <div className="mb-6">
        <h1 className="text-xl font-bold text-gray-900">智能书签</h1>
        <p className="text-sm text-gray-500">设置管理中心</p>
      </div>

      <ul className="space-y-1">
        {navItems.map((item) => (
          <li key={item.id}>
            <button
              onClick={() => {
                setActiveTab(item.id);
                window.location.hash = item.id;
              }}
              className={cn(
                "w-full flex items-center gap-3 px-3 py-2 rounded-lg transition-colors",
                activeTab === item.id
                  ? "bg-purple-100 text-purple-700"
                  : "text-gray-700 hover:bg-gray-100"
              )}
            >
              <item.icon className="w-5 h-5" />
              <span className="flex-1 text-left">{item.label}</span>
              {item.badge && (
                <span className="px-2 py-0.5 bg-red-500 text-white text-xs rounded-full">
                  {item.badge}
                </span>
              )}
            </button>
          </li>
        ))}
      </ul>
    </nav>
  );
}
```

### 页面路由组件

```tsx
export function SettingsPage() {
  const [activeTab, setActiveTab] = useState('dashboard');

  const renderContent = () => {
    switch (activeTab) {
      case 'dashboard':
        return <DashboardSettings />;
      case 'bookmarks':
        return <BookmarksSettings />;
      case 'ai':
        return <AISettings />;
      case 'organizer':
        return <OrganizerSettings />;
      case 'sync':
        return <SyncSettings />;
      case 'stats':
        return <StatsSettings />;
      case 'health':
        return <HealthSettings />;
      case 'advanced':
        return <AdvancedSettings />;
      case 'about':
        return <AboutSettings />;
      default:
        return <DashboardSettings />;
    }
  };

  return (
    <SettingsLayout>
      {renderContent()}
    </SettingsLayout>
  );
}
```

---

## 🎨 样式设计

### CSS 框架
- Tailwind CSS
- 响应式设计
- 深色模式支持

### 布局断点
```css
/* 移动端 */
@media (max-width: 768px) {
  .sidebar { display: none; }
  .main { margin-left: 0; }
}

/* 平板 */
@media (min-width: 769px) and (max-width: 1024px) {
  .sidebar { width: 200px; }
}

/* 桌面 */
@media (min-width: 1025px) {
  .sidebar { width: 256px; }
}
```

### 深色模式
```tsx
<div className="dark:bg-gray-900 dark:text-white">
  {/* 内容 */}
</div>
```

---

## 🔧 配置变更

### WXT 配置

```typescript
// wxt.config.ts
export default defineConfig({
  manifest: {
    name: '智能书签',
    options_page: 'settings.html',  // 修改为新的设置页面
  },
  entrypoints: {
    settings: 'src/entrypoints/settings/main.tsx',
  },
});
```

### 路由配置

```typescript
// 定义所有设置页面路由
export const SETTINGS_ROUTES = {
  dashboard: '/settings.html',
  bookmarks: '/settings.html#bookmarks',
  ai: '/settings.html#ai',
  organizer: '/settings.html#organizer',
  sync: '/settings.html#sync',
  stats: '/settings.html#stats',
  health: '/settings.html#health',
  advanced: '/settings.html#advanced',
  about: '/settings.html#about',
} as const;
```

---

## 📱 交互设计

### 导航流程

1. **初始加载**
   - 默认显示仪表盘
   - 高亮当前页面

2. **页面切换**
   - 点击侧边栏项
   - URL hash 更新
   - 内容区动态加载对应组件
   - 动画过渡效果

3. **浏览器前进/后退**
   - 监听 `hashchange` 事件
   - 自动切换到对应页面

### 快速操作

**键盘快捷键**:
- `Alt + Left/Right`: 切换页面
- `Ctrl + K`: 快速搜索
- `Escape`: 返回仪表盘

---

## 🔍 调试功能集成

### 调试面板位置

将现有的 `DebugPanel` 组件整合到两个地方：

1. **仪表盘页面**
   - 显示简化的系统状态
   - 快速操作按钮

2. **高级设置页面**
   - 完整的调试面板
   - 详细的日志输出
   - 高级调试功能

### 错误捕获

```tsx
class ErrorBoundary extends React.Component {
  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error('Settings Error:', error, errorInfo);
    // 上报错误
  }

  render() {
    if (this.state.hasError) {
      return <ErrorFallback error={this.state.error} />;
    }
    return this.props.children;
  }
}
```

---

## 📊 数据流设计

```
┌─────────────┐
│  Settings   │
│   Page      │
└──────┬──────┘
       │
       ├── 读取/写入配置
       │   ↓
┌─────────────┐
│ chrome.storage │
│    .local     │
└─────────────┘
       │
       ├── 数据库操作
       │   ↓
┌─────────────┐
│  IndexedDB  │
└─────────────┘
       │
       ├── Chrome API
       │   ↓
┌─────────────┐
│  Bookmarks  │
│    API      │
└─────────────┘
```

---

## 🎯 实施计划

### Phase 1: 基础架构 (优先级: 高)
- [ ] 创建 settings.html 入口
- [ ] 实现 SettingsLayout 布局
- [ ] 实现 SidebarNav 侧边栏
- [ ] 实现路由逻辑
- [ ] 迁移现有组件到新结构

### Phase 2: 页面内容 (优先级: 高)
- [ ] Dashboard 仪表盘页面
- [ ] AISettings AI 设置页面 (整合 DeepSeekConfig)
- [ ] OrganizerSettings 整理页面 (整合 BookmarksOrganizer)
- [ ] 调试面板整合

### Phase 3: 其他页面 (优先级: 中)
- [ ] BookmarksSettings 书签管理页面
- [ ] SyncSettings 同步设置页面
- [ ] StatsSettings 统计页面
- [ ] HealthSettings 健康检查页面
- [ ] AdvancedSettings 高级设置页面
- [ ] AboutSettings 关于页面

### Phase 4: 优化 (优先级: 低)
- [ ] 添加过渡动画
- [ ] 优化加载性能
- [ ] 添加搜索功能
- [ ] 完善移动端适配

---

## 💡 设计优势

### 1. 更好的用户体验
- **清晰的导航**: 侧边栏一目了然
- **快速定位**: 直接跳转到需要的功能
- **减少滚动**: 每个页面内容简洁

### 2. 更好的可维护性
- **模块化**: 每个页面独立组件
- **职责单一**: 每个页面只负责一个功能
- **易于调试**: 问题快速定位到具体页面

### 3. 更好的扩展性
- **新功能**: 添加新页面很容易
- **重构**: 修改单个页面不影响其他
- **A/B 测试**: 可以单独测试某个功能

### 4. 更专业的界面
- **类似系统设置**: 符合用户习惯
- **信息架构**: 清晰的功能分类
- **视觉层次**: 重要内容突出显示

---

## 📐 页面尺寸和性能

### 性能指标
- **首屏加载**: < 1s
- **页面切换**: < 100ms
- **内存占用**: < 50MB
- **包体积**: +50KB (新增路由和导航)

### 优化措施
1. **代码分割**: 每个页面单独加载
2. **懒加载**: 非关键组件延迟加载
3. **缓存**: 配置数据本地缓存
4. **防抖**: 搜索和自动保存使用防抖

---

## 🚀 总结

这个设计方案将设置页面从单一的长页面改造为模块化的多页面系统，带来：

✅ **更好的可用性**: 功能清晰，导航方便
✅ **更好的可维护性**: 模块独立，易于调试
✅ **更好的扩展性**: 新增功能容易
✅ **更好的专业性**: 界面美观，体验流畅

**下一步**: 等待设计确认后，使用 `/sc:implement` 开始实现。

---

## 附录: 技术选型

### 状态管理
- React Hooks (useState, useEffect)
- Zustand (如需全局状态)

### 路由
- Hash Router (无额外依赖)
- 或 Chrome Extension Navigation API

### UI 库板
- Lucide React (图标)
- Tailwind CSS (样式)
- Shadcn/ui (组件)

### 动画
- Framer Motion (过渡动画)
- 或 CSS Transitions (轻量级)
